# SEB model code directory
This directory contains only the core code for running the surface energy balance model. The model function is in `seb.m`, and the other m files are various helper functions.
